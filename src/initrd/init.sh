# Shiro init script
# Each line is executed by the shell at boot. Lines starting with '#'
# are comments and are ignored. Empty lines are also ignored.
clear
cat motd.txt
echo Type help to see all commands.
